# Banco editorial de preguntas

Guardar aquí una pregunta por archivo JSON cuando se migre o cree contenido. Un archivo publicable debe superar `npm run content:validate` y los checklists normativos.

El contenido que todavía solo vive en Moodle o en código se considera pendiente de migración, aunque funcione en producción.
